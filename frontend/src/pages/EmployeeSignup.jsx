import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ArrowLeft } from 'lucide-react';
import FormInput from '../components/FormInput';
import { signupEmployee } from '../services/api';
export default function EmployeeSignup() { const [form, setForm] = useState({ name:'', employeeId:'', email:'', password:'', whatsapp:'' }); const [error, setError] = useState(''); const navigate = useNavigate(); const update = (e) => setForm({...form, [e.target.name]:e.target.value}); const submit = async (e) => { e.preventDefault(); try { await signupEmployee(form); navigate('/employee/login'); } catch (err) { setError(err.message); } }; return <main className="min-h-screen bg-slate-50 px-5 py-10"><div className="mx-auto max-w-lg"><Link to="/employee/login" className="mb-8 inline-flex items-center gap-2 text-sm font-bold text-slate-500"><ArrowLeft size={16}/> Back to login</Link><div className="rounded-3xl border border-slate-200 bg-white p-7 shadow-card"><h1 className="text-2xl font-black text-slate-950">Create employee account</h1><p className="mt-2 text-sm text-slate-500">Use fictional details for this frontend demo.</p><form onSubmit={submit} className="mt-7 grid gap-4 sm:grid-cols-2"><FormInput label="Full Name" name="name" value={form.name} onChange={update} required/><FormInput label="Employee ID" name="employeeId" value={form.employeeId} onChange={update} required/><FormInput label="Email" name="email" type="email" value={form.email} onChange={update} required/><FormInput label="Password" name="password" type="password" value={form.password} onChange={update} required/><FormInput label="WhatsApp Number" name="whatsapp" value={form.whatsapp} onChange={update} required/><div className="sm:col-span-2">{error && <p className="mb-3 rounded-xl bg-rose-50 p-3 text-sm font-semibold text-rose-700">{error}</p>}<button className="w-full rounded-xl bg-indigo-600 px-4 py-3.5 text-sm font-bold text-white">Create Account</button></div></form></div></div></main>; }
