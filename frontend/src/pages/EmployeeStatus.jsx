import React from 'react';
import { Link } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
import { useApp } from '../context/AppContext';
export default function EmployeeStatus(){ const {user}=useAuth(); const {getEmployee}=useApp(); const e=getEmployee(user.id)||getEmployee(user.employeeId); return <main className="min-h-screen bg-slate-50 px-5 py-10"><div className="mx-auto max-w-3xl"><Link to="/employee/dashboard" className="text-sm font-bold text-indigo-600">← Dashboard</Link><h1 className="mt-7 text-3xl font-black text-slate-950">Assessment status</h1><div className="mt-6 grid gap-4 border border-slate-200 bg-white p-6 sm:grid-cols-2">{[['Employee Name',e?.name||user.name],['Employee ID',e?.employeeId||user.employeeId],['Selected HR',e?.hrName||'Not selected'],['Submission Status',e?'Submitted':'Not Submitted'],['Review Status',e?.reviewStatus||'Pending'],['Final Decision',e?.decision||'Awaiting HR Decision']].map(([a,b])=><div key={a}><p className="text-xs font-bold uppercase tracking-widest text-slate-400">{a}</p><p className="mt-1 font-extrabold text-slate-800">{b}</p></div>)}</div><div className="mt-6 border border-slate-200 bg-white p-6"><h2 className="font-extrabold text-slate-900">Review timeline</h2><div className="mt-5 space-y-4">{['Submitted','Received','Under Review','Reviewed','Decision Made'].map((item,index)=><div className="flex items-center gap-3" key={item}><span className={`h-3 w-3 rounded-full ${e && (e.decision || index < 2) ? 'bg-indigo-600' : 'bg-slate-200'}`}/><span className="text-sm font-bold text-slate-600">{item}</span></div>)}</div></div></div></main>; }
